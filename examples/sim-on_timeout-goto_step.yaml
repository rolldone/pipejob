pipeline:
  name: on-timeout-goto-step
  jobs:
    - name: job1
      steps:
        - name: slow
          type: command
          command: "sh -c 'echo start; sleep 2; echo finish'"
          timeout: "1s"
          on_timeout: "goto_step"
          on_timeout_step: "recover"

        - name: recover
          type: command
          command: "echo recovered after timeout"
